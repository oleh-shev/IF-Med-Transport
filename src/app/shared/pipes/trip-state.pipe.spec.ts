import { TripStatePipe } from './trip-state.pipe';

describe('TripStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TripStatePipe();
    expect(pipe).toBeTruthy();
  });
});
