import { ReservationStatePipe } from './reservation-state.pipe';

describe('ReservationStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReservationStatePipe();
    expect(pipe).toBeTruthy();
  });
});
